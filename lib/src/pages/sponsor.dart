import 'package:flutter/material.dart';

class SponsorPage extends StatelessWidget {
  const SponsorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sponsor', style: TextStyle(fontSize: 24)),
    );
  }
}
