import 'package:flutter/material.dart';

class RiddlesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Riddles', style: TextStyle(fontSize: 24)),
    );
  }
}
